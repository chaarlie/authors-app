import { EditableInputDirective } from './editable-input.directive';

describe('EditableInputDirective', () => {
  it('should create an instance', () => {
    const directive = new EditableInputDirective();
    expect(directive).toBeTruthy();
  });
});
